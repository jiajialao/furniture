spring:
  #datasource
  datasource:
    url: jdbc:mysql:///TextbookSystem?serverTimezone=UTC&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
server:
  port: 9900
